spring:
  datasource:
    password:
    url: jdbc:mysql://localhost:3306/demo_real
    username: root
  jpa:
      generate-ddl: true
      hibernate:
        ddl-auto: update
      database-platform: org.hibernate.dialect.MySQLDialect
      show-sql: true
